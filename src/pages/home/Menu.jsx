import SectionTitle from "../../shared-components/SectionTitle";

const Menu = () => {
  return (
    <div>
      <SectionTitle
        subHeading={"---Check it out---"}
        heading={"FROM OUR MENU"}
      ></SectionTitle>
    </div>
  );
};

export default Menu;
